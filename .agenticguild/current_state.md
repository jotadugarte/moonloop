# Agentic Guild — current state

- <active_task_pointer>.agenticguild/active_sessions/task_bug-menus-combobox-agrupado-y-filtro.md</active_task_pointer>
- **Phase:** start-task (Context Initialization)
- **Phase:** start-task (Execution — Iterative TDD)
- **Step:** 3.1 Write failing test (Step 1 of plan)
- **Context:** Added a new Selenium system spec asserting the picker shows the selected dish name in its closed state after autosave (`spec/system/menus_autosave_spec.rb`) — expected to fail with current UI. User constraint: do not run bundle/rails/rspec commands; provide copy-paste commands only (no `cd`). Also provide commit title + description at end of each step.
- **Next:** User runs the spec to confirm failure, then proceed to implementation to make it pass (Step 3.2).

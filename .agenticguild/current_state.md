# Agentic Guild — current state

- <active_task_pointer>.agenticguild/active_sessions/task_bug-menus-combobox-agrupado-y-filtro.md</active_task_pointer>
- **Phase:** start-task (Context Initialization)
- **Phase:** start-task (Execution — Iterative TDD)
- **Step:** 3.2 Implement to make test pass
- **Context:** Updated dish picker so the filter input shows the selected dish name after autosave, and clears itself on focus when it matches the selected value (so opening the picker shows all options). Updated session plan: Step 1 marked completed. User constraint: do not run commands; provide copy-paste commands only and include commit title + description at end of each step.
- **Next:** User runs `bin/rspec spec/system/menus_autosave_spec.rb` to confirm green, then proceed to next pending step (Step 2/3/4 in session plan).

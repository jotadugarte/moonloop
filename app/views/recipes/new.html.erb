<% content_for :title, t("recipes.new.title") %>

<h1 class="text-2xl font-bold mb-4"><%= t("recipes.new.heading") %></h1>

<%= render "form" %>

<p class="mt-6">
  <%= link_to t("recipes.new.back"), recipes_path, class: "text-blue-600 underline" %>
</p>

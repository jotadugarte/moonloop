<% content_for :title, t("plan_assignments.new.title") %>

<h1 class="text-2xl font-bold mb-4"><%= t("plan_assignments.new.heading") %></h1>

<%= render "form" %>

<p class="mt-6">
  <%= link_to t("plan_assignments.new.back"), edit_plan_path(@plan), class: "text-blue-600 underline" %>
</p>
